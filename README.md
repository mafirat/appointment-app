# Loodos - Appointment App

* Uygulama başlatmak için: `yarn start`
* Sadece arayüz başlatmak için: `yarn reactstart`
* Sadece API başlatmak için: `yarn apiserver`


## admin kullanıcısı
mail: __admin@admin.com__
password __123456__
