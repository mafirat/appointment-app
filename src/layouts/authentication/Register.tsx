import React from "react";

interface IProps {

}

export const Register: React.FunctionComponent<IProps> = (props) => {
    return (
        <div>
            Register Page
        </div>
    );
}

export default Register;