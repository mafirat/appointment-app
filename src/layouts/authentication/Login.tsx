import React from "react";

interface IProps {

}

export const Login: React.FunctionComponent<IProps> = (props) => {
    return (
        <div>
            Login Page
        </div>
    );
}

export default Login;